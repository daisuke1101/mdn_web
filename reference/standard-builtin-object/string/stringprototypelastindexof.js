const paragraph = 'The quick brown fox jumps over the lazy dog. if the dog barked, was it really lazy?'

const searchTerm = 'dog';

console.log('The index of the 1st "' + searchTerm + '" from the end is ' + paragraph.lastIndexOf(searchTerm));  // The index of the 1st "dog" from the end is 52